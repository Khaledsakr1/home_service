import 'package:flutter/material.dart';

class SettingsmyprofileAddress extends StatelessWidget {
  const SettingsmyprofileAddress({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}