import 'package:flutter/material.dart';

class StartNewProjectWorker extends StatelessWidget {
  const StartNewProjectWorker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}